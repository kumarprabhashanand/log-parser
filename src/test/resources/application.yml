spring:
  jpa:
    database: HSQL
    show-sql: true
    hibernate:
      ddl-auto: create-drop

  datasource:
    url: jdbc:hsqldb:file:testdata/testdb
    driverClassName: org.hsqldb.jdbc.JDBCDriver
